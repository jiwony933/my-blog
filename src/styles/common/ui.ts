import styled from '@emotion/styled';

export const ContentTitle = styled.h1`
  font-size: 22px;
  font-weight: 600;
  word-break: break-all;
`;

export const ContentH2Title = styled.h1`
  font-size: 20px;
  font-weight: 500;
  white-space: nowrap;
`;

export const RequiredMessage = styled.div`
  font-size: 12px;
  font-weight: 600;
  color: var(--red400);
`;

export const Required = styled.span`
  font-size: 12px;
  font-weight: 600;
  color: var(--red400);
`;
